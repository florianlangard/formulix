{% extends 'base.html.twig' %}
{% block active_users %}{% endblock %}
{% block active_results %}active{% endblock %}

{% block body %}
    <h1>Result</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ result.id }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ result.time }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_result_index') }}">back to list</a>

    <a href="{{ path('back_result_edit', {'id': result.id}) }}">edit</a>

    {{ include('back/result/_delete_form.html.twig') }}
{% endblock %}
